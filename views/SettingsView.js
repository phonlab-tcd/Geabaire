import { Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

export default function SettingsView() {
    return (
        <SafeAreaView>
            <Text>Settings Page</Text>
            {
                // TODO: add way out of settings page.
            }
        </SafeAreaView>
    )
}